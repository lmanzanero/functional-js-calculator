import { one, two , three, four, five, six, seven, eight, nine, ten } from './nums';

test('Function one() should return 1', () => {
    expect(one()).toBe(1);
});

test('Function two() should return 2', () => {
    expect(two()).toBe(2);
});

test('Function three() should return 3', () => {
    expect(three()).toBe(3);
});

test('Function four() should return 4', () => {
    expect(four()).toBe(4);
});

test('Function five() should return 5', () => {
    expect(five()).toBe(5);
});

test('Function six() should return 6', () => {
    expect(six()).toBe(6);
});

test('Function seven() should return 7', () => {
    expect(seven()).toBe(7);
});

test('Function eight() should return 8', () => {
    expect(eight()).toBe(8);
});

test('Function nine() should return 4', () => {
    expect(nine()).toBe(9);
});

test('Function ten() should return 4', () => {
    expect(ten()).toBe(10);
});