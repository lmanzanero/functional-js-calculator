export const one = (operation) => { 
    if(operation === null || operation === "" || operation === undefined) {
        return 1;
    } else {
        let result = eval(`1 ${operation}`);
        return result;
    }
}

export const two = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 2;
    } else {
        let result = eval(`2 ${operation}`);
        return result;
    }
}

export const three = (operation) => {
    if(operation === null || operation === "" || operation === undefined) {
        return 3;
    } else {
        let result = eval(`3 ${operation}`);
        return result;
    }
}

export const four = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 4;
    } else {
        let result = eval(`4 ${operation}`);
        return result;
    }
}

export const five = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 5;
    } else {
        let result = eval(`5 ${operation}`);
        return result;
    }
}

export const six = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 6;
    } else {
        let result = eval(`6 ${operation}`);
        return result;
    }
}

export const seven = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 7;
    } else {
        let result = eval(`7 ${operation}`);
        return result;
    }
}

export const eight = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 8;
    } else {
        let result = eval(`8 ${operation}`);
        return result;
    }
}

export const nine = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 9;
    } else {
        let result = eval(`9 ${operation}`);
        return result;
    }
}

export const ten = (operation) => {
    if(operation == null || operation === "" || operation === undefined) {
        return 10;
    } else {
        let result = eval(`10 ${operation}`);
        return result;
    }
}

